# 🚀 Vercel 배포 단계별 가이드

## 1️⃣ 랜딩페이지 배포

### Step 1: Vercel 접속 및 로그인
1. 브라우저에서 https://vercel.com 접속
2. "Sign Up" 또는 "Log In" 클릭 (GitHub 계정으로 로그인 권장)

### Step 2: 새 프로젝트 생성
1. 대시보드에서 **"Add New..."** 버튼 클릭
2. **"Project"** 선택
3. GitHub 계정 연결 (처음이면 GitHub 권한 허용)

### Step 3: 저장소 선택
1. 저장소 목록에서 **`iyb030106-stack/areum.`** 선택
2. **"Import"** 버튼 클릭

### Step 4: 프로젝트 설정
프로젝트 설정 화면에서 아래와 같이 입력:

```
프로젝트 이름: areum-landing (또는 원하는 이름)

Framework Preset: Vite

Root Directory: ./

Build Command: npm run build:landing

Output Directory: dist-landing

Install Command: npm install
```

### Step 5: 환경 변수 (선택사항)
"Environment Variables" 섹션에서 추가:
- Key: `VITE_STORE_URL`
- Value: `https://store.areum.com` (또는 Store 배포 후 실제 도메인)

### Step 6: 배포
1. **"Deploy"** 버튼 클릭
2. 배포가 완료될 때까지 대기 (약 1-2분)

### Step 7: 도메인 설정
1. 배포 완료 후 프로젝트 페이지에서 **"Settings"** 탭 클릭
2. **"Domains"** 메뉴 클릭
3. 원하는 도메인 입력 (예: `areum.com`)
4. DNS 설정 안내에 따라 도메인 연결

---

## 2️⃣ Store 배포

### Step 1: 새 프로젝트 생성
1. Vercel 대시보드에서 **"Add New..."** → **"Project"** 클릭
2. **같은 저장소** `iyb030106-stack/areum.` 선택
3. **"Import"** 버튼 클릭

### Step 2: 프로젝트 설정
프로젝트 설정 화면에서 아래와 같이 입력:

```
프로젝트 이름: areum-store (또는 원하는 이름)

Framework Preset: Vite

Root Directory: ./

Build Command: npm run build:store

Output Directory: dist-store

Install Command: npm install
```

### Step 3: 환경 변수 (선택사항)
"Environment Variables" 섹션에서 추가:
- Key: `VITE_LANDING_URL`
- Value: `https://areum.com` (또는 랜딩페이지 실제 도메인)

### Step 4: 배포
1. **"Deploy"** 버튼 클릭
2. 배포가 완료될 때까지 대기

### Step 5: 도메인 설정
1. 배포 완료 후 프로젝트 페이지에서 **"Settings"** 탭 클릭
2. **"Domains"** 메뉴 클릭
3. 서브도메인 입력 (예: `store.areum.com`)
4. DNS 설정 안내에 따라 도메인 연결

---

## ✅ 배포 확인

### 랜딩페이지
배포 후 Vercel에서 제공하는 URL로 접속하여 확인:
- 예: `https://areum-landing.vercel.app`

### Store
배포 후 Vercel에서 제공하는 URL로 접속하여 확인:
- 예: `https://areum-store.vercel.app`

---

## 🔗 도메인 연결 후

각 프로젝트의 Settings → Domains에서:
- 랜딩페이지: `areum.com`, `www.areum.com` 등
- Store: `store.areum.com`, `shop.areum.com` 등

---

## 🆘 문제 발생 시

### 빌드 실패
1. Vercel 대시보드에서 "Deployments" 탭 클릭
2. 실패한 배포의 로그 확인
3. 로컬에서 테스트: `npm run build:landing` 또는 `npm run build:store`

### 도메인 연결 안 됨
1. Settings → Domains에서 DNS 설정 확인
2. 도메인 제공업체에서 DNS 레코드 추가
3. DNS 전파 대기 (최대 24시간 소요)

### 환경 변수 적용 안 됨
1. Settings → Environment Variables 확인
2. 변수 재추가 후 프로젝트 재배포

---

## 📸 주요 화면 설명

### 프로젝트 설정 화면 구성:
```
┌─────────────────────────────────────┐
│ Project Name: [입력]                │
│                                     │
│ Framework Preset: [Vite 선택]       │
│                                     │
│ Root Directory: [./]                │
│                                     │
│ Build Command: [입력 필수!]         │
│ Output Directory: [입력 필수!]      │
│                                     │
│ Install Command: [npm install]      │
│                                     │
│ [Deploy] 버튼                       │
└─────────────────────────────────────┘
```

**중요**: Build Command와 Output Directory는 **반드시 입력**해야 합니다!



